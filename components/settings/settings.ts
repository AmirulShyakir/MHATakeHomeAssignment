/**
 * The settings.ts file will be used to store common settings variables, such as constants.
 * For values that need to be changed depending on environment, do consider looking at environment variables instead.
 */

// Pixel threshold to be considered as a small screen for alternate layout
export const SMALL_SCREEN_BREAKPOINT = 1024;
